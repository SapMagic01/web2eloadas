<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Irányítás a public mappába
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>